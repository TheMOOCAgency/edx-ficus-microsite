Welcome to ${course.display_name_with_default_escaped}.

To get started, please visit https://${site_name}. The login information for your account follows.

email: ${email_address}
password: ${password}

It is recommended to change the password.

Sincerely, the team of ${course.display_name_with_default_escaped}




Bienvenido a ${course.display_name_with_default_escaped}.

Para comenzar, vaya a https://${site_name}.

correo electrónico: ${email_address}
contraseña: ${password}

Se recomienda cambiar la contraseña.

Atentamente, el equipo de ${course.display_name_with_default_escaped}




Bienvenue à ${course.display_name_with_default_escaped}. 

Pour commencer, merci de vous rendre sur le lien suivant https://${site_name} avec vos identifiants de connexion :

courriel : ${email_address}
mot de passe : ${password}

Il est recommandé de changer son mot d epasse.

Cordialement, l'équipe de ${course.display_name_with_default_escaped}